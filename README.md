# GTA Cayo Perico Calculator

A lightweight, dependency-free payout planner for GTA Online's Cayo Perico heist.

## Run locally

Open `index.html` directly in a browser, or serve the directory with any static file server.

```sh
npx serve .
```

